# Example .env for LeaderBoard_Frontend (Vite)
# Copy to `.env` or `.env.local` and adjust if needed

# URL for backend API (used by import.meta.env.VITE_API_URL)
VITE_API_URL=http://localhost:5000/api

# Optional:
# VITE_SOME_OTHER_KEY=your_value